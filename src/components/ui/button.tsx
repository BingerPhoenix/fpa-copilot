import React from "react"; import { cn } from "@/lib/utils"; export function Button({ children, variant = "default", size = "default", className, ...props }: any) { return <button className={cn("px-4 py-2 rounded-md font-medium", variant === "financial" ? "bg-blue-600 text-white hover:bg-blue-700" : variant === "outline" ? "border border-gray-300 bg-white hover:bg-gray-50" : "bg-gray-900 text-white hover:bg-gray-800", size === "sm" ? "px-3 py-1 text-sm" : "", className)} {...props}>{children}</button> }
